name: Deploy to Production

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy Backend & Admin Panel
    runs-on: self-hosted  # 🎯 RODA NO SEU SERVIDOR!

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Stop containers
        run: |
          cd /opt/devocionaldiario
          echo "🛑 Stopping all containers..."
          docker-compose down
          echo "✅ Containers stopped"

      - name: Pull latest code
        run: |
          cd /opt/devocionaldiario
          echo "📥 Pulling latest code from main branch..."
          git fetch origin
          git reset --hard origin/main
          echo "✅ Code updated"

      - name: Build backend image
        run: |
          cd /opt/devocionaldiario
          echo "🔨 Building backend Docker image..."
          docker-compose build --no-cache backend
          echo "✅ Backend image built"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Build Admin Panel
        run: |
          cd admin-panel
          echo "🔨 Building admin panel..."
          npm install
          npm run build
          echo "✅ Admin panel built"
        env:
          VITE_API_URL: https://api.gladiussistemas.com.br

      - name: Deploy admin panel files (containers are stopped)
        run: |
          cd /opt/devocionaldiario
          echo "📦 Deploying admin panel files..."
          # Remove old dist folder completely (containers are down, so it's safe)
          sudo rm -rf admin-panel/dist
          # Copy new build
          sudo cp -r ${{ github.workspace }}/admin-panel/dist admin-panel/
          # Set proper permissions for nginx
          sudo chown -R $USER:$USER admin-panel/dist
          sudo chmod -R 755 admin-panel/dist
          echo "✅ Admin panel deployed"

      - name: Start all containers
        run: |
          cd /opt/devocionaldiario
          echo "🚀 Starting all containers..."
          docker-compose up -d
          echo "✅ Containers started"

      - name: Wait for services
        run: |
          echo "⏳ Waiting for services to be ready..."
          sleep 15

      - name: Verify backend health
        run: |
          echo "🏥 Checking backend health..."
          # Retry health check up to 5 times with 5 second intervals
          max_attempts=5
          attempt=1
          while [ $attempt -le $max_attempts ]; do
            echo "Attempt $attempt/$max_attempts..."
            if curl -f http://localhost:3000/health 2>/dev/null; then
              echo "✅ Backend is healthy"
              exit 0
            fi
            if [ $attempt -lt $max_attempts ]; then
              echo "Backend not ready yet, waiting 5 seconds..."
              sleep 5
            fi
            attempt=$((attempt + 1))
          done
          echo "❌ Backend health check failed after $max_attempts attempts"
          exit 1

      - name: Show container status
        run: |
          cd /opt/devocionaldiario
          echo "📊 Container status:"
          docker-compose ps

      - name: Show backend logs
        if: always()
        run: |
          cd /opt/devocionaldiario
          echo "📋 Recent backend logs:"
          docker-compose logs --tail=50 backend

      - name: Cleanup build artifacts
        run: |
          cd admin-panel
          echo "🧹 Cleaning up temporary files..."
          rm -rf node_modules
          echo "✅ Build artifacts cleaned"

      - name: Cleanup Docker resources
        run: |
          echo "🧹 Removing unused Docker images..."
          docker image prune -af --filter "until=24h" || true
          echo "✅ Docker cleanup completed"

      - name: Deploy Summary
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🎉 DEPLOYMENT COMPLETED SUCCESSFULLY!"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "✅ Backend: https://api.gladiussistemas.com.br"
          echo "✅ Admin Panel: https://admin.gladiussistemas.com.br"
          echo "✅ Landing Page: https://page.gladiussistemas.com.br"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
